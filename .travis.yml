language: python
cache: pip
python:
  - "3.7"
install:
  - "python3 -m pip install homeassistant==0.103.6"
script:
  - "mv secrets.yaml.sample secrets.yaml"
  - "touch fullchain.pem"
  - "touch privkey.pem"
  - "hass --version"
  - "hass -c . --script check_config | tee check.output"
  - '! less check.output | grep -q "Failed config"'
